<Window x:Class="ScadaGUI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SCADA - Prijava" Height="520" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Window.Background>
        <SolidColorBrush Color="#1976D2"/>
    </Window.Background>
    <Border Margin="30" Background="White" CornerRadius="8" Padding="30">
        <StackPanel>
            <TextBlock Text="SCADA Sistem" FontSize="22" FontWeight="Bold"
                       Foreground="#1976D2" HorizontalAlignment="Center" Margin="0,0,0,5"/>
            <TextBlock Text="Prijavite se da nastavite" FontSize="13" Foreground="#757575"
                       HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <TextBlock Text="Korisničko ime" FontSize="12" Foreground="#424242" Margin="0,0,0,4"/>
            <TextBox x:Name="UsernameBox" Height="36" FontSize="14" Padding="8,6"
                     BorderBrush="#BDBDBD" BorderThickness="1" Margin="0,0,0,12"
                     ToolTip="Unesite korisničko ime"/>
            <TextBlock Text="Lozinka" FontSize="12" Foreground="#424242" Margin="0,0,0,4"/>
            <PasswordBox x:Name="PasswordBox" Height="36" FontSize="14" Padding="8,6"
                         BorderBrush="#BDBDBD" BorderThickness="1" Margin="0,0,0,12"
                         ToolTip="Unesite lozinku"/>
            <TextBlock Text="Uloga" FontSize="12" Foreground="#424242" Margin="0,0,0,4"/>
            <ComboBox x:Name="RoleBox" Height="36" FontSize="14" Margin="0,0,0,20"
                      ToolTip="Izaberite ulogu">
                <ComboBoxItem Content="admin"/>
                <ComboBoxItem Content="operater"/>
                <ComboBoxItem Content="student"/>
                <ComboBoxItem Content="teacher"/>
            </ComboBox>
            <TextBlock x:Name="ErrorText" Foreground="Red" FontSize="12"
                       Margin="0,0,0,10" Visibility="Collapsed"/>
            <Button Content="Prijavi se" Height="40" FontSize="14" Background="#1976D2"
                    Foreground="White" BorderThickness="0" Cursor="Hand"
                    Click="LoginButton_Click" Margin="0,0,0,10">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="12,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <TextBlock Text="admin / Admin@123456789 / admin"
                       FontSize="10" Foreground="#9E9E9E" HorizontalAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>
