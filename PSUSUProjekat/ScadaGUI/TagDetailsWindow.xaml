<Window x:Class="ScadaGUI.Views.TagDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detalji taga" Height="500" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="TitleText" FontSize="18" FontWeight="Bold"
                   Foreground="#1976D2" Margin="0,0,0,12"/>

        <Border Grid.Row="1" Background="#F5F5F5" CornerRadius="4" Padding="12" Margin="0,0,0,12">
            <Grid x:Name="PropsGrid"/>
        </Border>

        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="36"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="#D32F2F" CornerRadius="4,4,0,0" Padding="10,0">
                    <TextBlock Text="Alarmi" FontSize="13" FontWeight="Bold"
                               Foreground="White" VerticalAlignment="Center"/>
                </Border>
                <DataGrid Grid.Row="1" x:Name="AlarmsGrid" AutoGenerateColumns="False"
                          IsReadOnly="False" CanUserAddRows="False" FontSize="12">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Granica"
                            Binding="{Binding LimitValue, StringFormat=F2}" Width="80"/>
                        <DataGridTextColumn Header="Smer" Binding="{Binding Direction}" Width="100"/>
                        <DataGridTextColumn Header="Poruka" Binding="{Binding Message}" Width="200"/>
                        <DataGridTextColumn Header="Stanje" Binding="{Binding State}" Width="100"/>
                        <DataGridTemplateColumn Header="Ukloni" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="X" Background="#D32F2F" Foreground="White"
                                            BorderThickness="0" Padding="6,3" Cursor="Hand"
                                            Click="RemoveAlarm_Click" Tag="{Binding Id}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Button Grid.Row="3" Content="Zatvori" Click="Close_Click" HorizontalAlignment="Right"
                Margin="0,12,0,0" Padding="20,8" Background="#1976D2" Foreground="White"
                BorderThickness="0" Cursor="Hand" FontSize="13"/>
    </Grid>
</Window>
